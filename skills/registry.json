{
  "$schema": "./spec/skill.schema.json",
  "skills": [
    {
      "id": "observability-and-env",
      "title": "Observability and Environment Setup",
      "summary": "Companion to tanstack-promptable-fullstack-app-template. Use when adding structured logging, centralized environment validation (runtime schemas), error-tracking bootstrap, or fixing env/shellSession leaks in a TanStack Start app. Teaches interface-first observability patterns: parse env once, inject via middleware, project a browser-safe shellSession, and keep process.env out of handlers. The reference app uses pino + Sentry behind ObservabilityService — swap vendors without changing middleware or handler contracts.",
      "projectName": "TanStack AI-Promptable Full-Stack Template",
      "projectSummary": "A production-ready TanStack Start template designed to make internal tools AI promptable by default.",
      "version": "1.7.0",
      "author": {
        "name": "Carlos Martin-Sanchez",
        "url": "https://github.com/carlosvin"
      },
      "license": "MIT",
      "homepage": "https://github.com/carlosvin/tanstack-fullstack-ai-template",
      "repository": "https://github.com/carlosvin/tanstack-fullstack-ai-template",
      "documentationUrl": "https://github.com/carlosvin/tanstack-fullstack-ai-template/blob/main/AGENTS.md",
      "status": "stable",
      "supportedTools": [
        {
          "id": "windsurf",
          "name": "Windsurf",
          "installMode": "native",
          "tested": true,
          "notes": "Reads `.agents/skills/` directly when the skill lives in the project."
        },
        {
          "id": "cursor",
          "name": "Cursor",
          "installMode": "copy",
          "tested": true,
          "notes": "Copy the generated skill folder into the global Cursor skills directory."
        },
        {
          "id": "claude-code",
          "name": "Claude Code",
          "installMode": "copy",
          "tested": true,
          "notes": "Copy the generated skill folder into the Claude Code skills directory."
        }
      ],
      "tags": [
        "observability",
        "logging",
        "sentry",
        "pino",
        "environment",
        "configuration",
        "tanstack-start"
      ],
      "capabilities": [
        "Interface-first observability — handlers depend on ObservabilityService, not a specific vendor",
        "Centralized runtime-validated env schemas with server/public split (src/env/) — reference uses Zod",
        "process.env read only once at module-level parse — no scattered env access",
        "Structured logger factory pattern (reference: pino via createModuleLogger / createServerLogger)",
        "Error-tracking bootstrap pattern (reference: Sentry via instrument.*.mts)",
        "instrument.env.shared.mts for a shared deployment env schema (bootstrap + TS callers)",
        "instrument.env.mts for strict bootstrap env validation using the shared deployment env schema",
        "instrument.shared.mts for reusable initSentry — callers pre-resolve all values",
        "instrument.server.mts as the dev --import entry; tsc emits instrument.*.mjs to .output/server for production",
        "Browser config via shellSession: route loaders call getBrowserShellSession — never import webEnv in client-shared modules",
        "Typed request context via middleware chaining: webEnvMiddleware injects serverEnv and shellSession through next({ context })",
        "No window.__ENV__ global"
      ],
      "triggers": [
        "add logging",
        "set up logging",
        "structured logging",
        "pino logger",
        "sentry init",
        "error tracking",
        "opentelemetry",
        "instrument server",
        "instrument.server.mts",
        "instrument.server.mjs",
        "env schema",
        "environment validation",
        "centralize observability",
        "createModuleLogger",
        "createServerLogger",
        "webEnv",
        "webServerEnv",
        "shellSession",
        "getBrowserShellSession",
        "webEnvMiddleware",
        "LOG_LEVEL",
        "SENTRY_DSN",
        "serverEnv leak",
        "window.__ENV__",
        "process.env in handler",
        "process.env in application code"
      ],
      "inputs": [
        "TanStack Start app (from this template or another)"
      ],
      "outputs": [
        "src/env/runtimeEnvSchema.ts with DeploymentEnv and LogLevel enums",
        "src/env/webEnv.ts with webServerEnv + shellSession singletons (parsed once at startup)",
        "src/utils/logger.ts with createModuleLogger factory (vendor-agnostic options)",
        "src/utils/serverLogger.ts with createServerLogger bound factory",
        "src/services/observability/ with ObservabilityService interface + swappable implementations",
        "instrument.env.shared.mts with the shared DeploymentEnvSchema for bootstrap + TS callers",
        "instrument.env.mts with resolveSentryBootstrapEnv helper",
        "instrument.shared.mts with initSentry (all values pre-resolved by caller)",
        "instrument.server.mts bootstrap entry and tsconfig.instrument.json emitting .mjs to .output/server"
      ],
      "constraints": [
        "process.env is read only in src/env/*.ts (schema parse) and in a single BootstrapEnvSchema.parse(process.env) call inside instrument.env.mts — never in application code",
        "Env and app meta are parsed once at module load (server startup) — never re-read process.env or package.json in handlers",
        "Logger options (logLevel, environment) are passed as arguments to createModuleLogger — never resolved inside the factory",
        "webEnvMiddleware injects ctx.context.serverEnv and shellSession via next({ context }); chain on server fns that need those fields",
        "Browser sees only getBrowserShellSession / shellSession via root loader — never serverEnv or window.__ENV__",
        "Module-scope env parse is for the server process lifetime on Node hosts; on edge SSR (e.g. Cloudflare Workers) read process.env inside handlers/middleware per TanStack Start env docs",
        "Dev runs Node with tsx preloaded then instrument.server.mts; production uses node --import on emitted .output/server/instrument.server.mjs",
        "instrument.env.mts validates bootstrap env in one place, importing the shared DeploymentEnvSchema (relative imports use .mjs extension for NodeNext emit mapping)",
        "Bootstrap NODE_ENV uses DeploymentEnvSchema.optional(); unsupported values fail fast instead of being trimmed, ignored, or coerced",
        "Bootstrap error-tracking DSN env var is read only in instrument.env.mts; values are passed through exactly as provided",
        "instrument.shared.mts receives dsn and environment as explicit pre-resolved arguments — no process.env inside",
        "DeploymentEnvSchema is defined once in instrument.env.shared.mts and re-exported from runtimeEnvSchema.ts for TypeScript callers",
        "Root logger is created lazily and only once per process — all module loggers are child() instances (reference: pino)",
        "Application code calls getObservability() / createServerLogger() — never imports @sentry/* or pino directly in handlers"
      ],
      "steps": [
        "Choose logging and error-tracking implementations behind ObservabilityService (reference app: pino + Sentry)",
        "Add chosen logger dependencies (reference: pino and pino-pretty)",
        "Create instrument.env.shared.mts with DeploymentEnvSchema",
        "Create src/env/runtimeEnvSchema.ts with DeploymentEnv, LogLevel, and optional preprocessors",
        "Create src/env/webEnv.ts — parse process.env once into webServerEnv; derive shellSession (public env + app from package.json)",
        "Create src/utils/logger.ts with createModuleLogger and lazy root logger",
        "Create src/utils/serverLogger.ts binding webServerEnv.ENV and LOG_LEVEL",
        "Create instrument.env.mts with resolveSentryBootstrapEnv() using the shared DeploymentEnvSchema",
        "Create instrument.shared.mts with initSentry (receives pre-resolved dsn + environment)",
        "Keep instrument.server.mts as a bootstrap entry that calls resolveSentryBootstrapEnv() + initSentry()",
        "Add tsconfig.instrument.json and extend build script with tsc -p tsconfig.instrument.json && cp package.json .output/server",
        "Update getObservability() to read shellSession.SENTRY_DSN instead of process.env",
        "Update middleware/auth.ts to read AUTH_HEADER_NAME from webServerEnv",
        "Add webEnvMiddleware that injects serverEnv and shellSession (chains auth)",
        "Add GET getBrowserShellSession that chains webEnvMiddleware and returns context.shellSession; load in root loader"
      ],
      "examples": [
        {
          "input": "Add structured logging to a server function",
          "output": "Import createServerLogger and call createServerLogger('myModule') — env and logLevel come from webServerEnv automatically"
        },
        {
          "input": "Set up error tracking for the server",
          "output": "Create instrument.env.mts + instrument.shared.mts; keep instrument.server.mts as a bootstrap entry; emit .mjs via tsc for production (reference implementation uses Sentry — swap via ObservabilityService)"
        },
        {
          "input": "Read environment variables safely in application code",
          "output": "Import webServerEnv from src/env/webEnv.ts (already validated), or read context.serverEnv in handlers — never read process.env directly in app code"
        },
        {
          "input": "Expose app name/version and public env to the client",
          "output": "Root loader calls getBrowserShellSession(); UI reads shellSession from loader data — never import webEnv or package.json in client components"
        }
      ],
      "companionSkills": [
        {
          "id": "tanstack-promptable-fullstack-app-template",
          "relationship": "parent",
          "summary": "Architecture contract for this template — schema layers, server boundaries, AI tools, and middleware-inferred request context. Install for all non-observability TanStack work."
        },
        {
          "id": "reference-tech-stack",
          "relationship": "companion",
          "summary": "Opinionated vendor map for this template's reference app. Install when matching the demo stack's concrete packages."
        }
      ],
      "source": "skills/src/observability-and-env.skill.yaml",
      "outputsByFormat": {
        "skill": ".agents/skills/observability-and-env/SKILL.md",
        "markdown": "skills/dist/observability-and-env.md"
      }
    },
    {
      "id": "reference-tech-stack",
      "title": "Reference Tech Stack (Opinionated Defaults)",
      "summary": "Companion to tanstack-promptable-fullstack-app-template. Documents this template's opinionated vendor choices — Zod, Mantine, lucide-react, MongoDB + seed, jose JWT auth, OpenAI adapter, pino + Sentry, react-markdown, Biome, Vitest, Playwright, Netlify — so agents implement against the reference app without baking those choices into the architecture skill.",
      "projectName": "TanStack AI-Promptable Full-Stack Template",
      "projectSummary": "A production-ready TanStack Start template designed to make internal tools AI promptable by default.",
      "version": "1.2.0",
      "author": {
        "name": "Carlos Martin-Sanchez",
        "url": "https://github.com/carlosvin"
      },
      "license": "MIT",
      "homepage": "https://github.com/carlosvin/tanstack-fullstack-ai-template",
      "repository": "https://github.com/carlosvin/tanstack-fullstack-ai-template",
      "documentationUrl": "https://github.com/carlosvin/tanstack-fullstack-ai-template/blob/main/AGENTS.md",
      "status": "stable",
      "supportedTools": [
        {
          "id": "windsurf",
          "name": "Windsurf",
          "installMode": "native",
          "tested": true,
          "notes": "Reads `.agents/skills/` directly when the skill lives in the project."
        },
        {
          "id": "cursor",
          "name": "Cursor",
          "installMode": "copy",
          "tested": true,
          "notes": "Copy the generated skill folder into the global Cursor skills directory."
        },
        {
          "id": "claude-code",
          "name": "Claude Code",
          "installMode": "copy",
          "tested": true,
          "notes": "Copy the generated skill folder into the Claude Code skills directory."
        }
      ],
      "tags": [
        "reference-stack",
        "opinionated",
        "mantine",
        "zod",
        "mongodb",
        "netlify",
        "tanstack-start"
      ],
      "capabilities": [
        "Opinionated vendor map for this template (UI, validation, DB, auth, AI, observability, tooling)",
        "Pointers to AGENTS.md sections for operational how-to",
        "Clear separation from the architecture skill (patterns) and observability-and-env (env/logging recipes)",
        "Guidance for forking teams that want to swap a vendor without rewriting architecture"
      ],
      "triggers": [
        "reference tech stack",
        "opinionated stack",
        "which UI library",
        "use Mantine",
        "use Zod",
        "MongoDB repository",
        "lucide icons",
        "Biome lint",
        "Netlify deploy",
        "template defaults",
        "stack choices",
        "AppShell",
        "Mantine navbar"
      ],
      "inputs": [
        "TanStack Start app built from this template (or forking its stack)"
      ],
      "outputs": [
        "Clear map of chosen packages and where they live in the repo",
        "Pointers to AGENTS.md for UI, auth, AI, testing, and deploy detail"
      ],
      "constraints": [
        "Do not treat this skill as architecture — patterns and invariants live in tanstack-promptable-fullstack-app-template",
        "When swapping a vendor in a fork, update this skill (or drop it) and AGENTS.md; keep the architecture skill vendor-agnostic",
        "Prefer one library per concern (one UI kit, one validator, one icon set)",
        "Observability setup details stay in observability-and-env — this skill only names the chosen vendors"
      ],
      "steps": [
        "Confirm you are working in this template (or intentionally adopting its defaults)",
        "Use the Stack map below for package and file locations",
        "Follow AGENTS.md sections linked for each concern",
        "For env/logging/Sentry wiring, also load observability-and-env",
        "If forking away from a default, replace the implementation behind the existing interface and update this skill"
      ],
      "examples": [
        {
          "input": "Which UI library should I use in this template?",
          "output": "Mantine (@mantine/core, @mantine/hooks, @mantine/notifications) with lucide-react icons — see AGENTS.md §3 and Stack map in this skill."
        },
        {
          "input": "Can I use ArkType instead of Zod?",
          "output": "Architecture allows it; this reference stack uses Zod. To switch, pick one validator app-wide, update schemas, and revise this skill / AGENTS.md."
        },
        {
          "input": "Where is MongoDB wired?",
          "output": "Behind ReadRepository / WritableRepository — Mongo + seed implementations in src/services/repository/; factory in getRepository.server.ts."
        },
        {
          "input": "Make the header work on small screens in this template",
          "output": "Apply optional mobile first with Mantine — `base` then larger breakpoints, AppShell + Burger + NavLink for navigation (AGENTS.md §3). Ask if the rewrite looks hard."
        }
      ],
      "companionSkills": [
        {
          "id": "tanstack-promptable-fullstack-app-template",
          "relationship": "parent",
          "summary": "Architecture contract — schema layers, server boundaries, AI tools, and middleware-inferred request context. Vendor-agnostic; install for all TanStack work."
        },
        {
          "id": "observability-and-env",
          "relationship": "companion",
          "summary": "Env parse, logger factories, and error-tracking bootstrap. This stack skill names pino + Sentry; that companion owns the setup recipe."
        }
      ],
      "source": "skills/src/reference-tech-stack.skill.yaml",
      "outputsByFormat": {
        "skill": ".agents/skills/reference-tech-stack/SKILL.md",
        "markdown": "skills/dist/reference-tech-stack.md"
      }
    },
    {
      "id": "tanstack-promptable-fullstack-app-template",
      "title": "TanStack Promptable Fullstack App Template",
      "summary": "Use when scaffolding a new TanStack Start project, adding domain entities, implementing the interface-first repository pattern with AI-promptable tools, fixing nested layout routes that duplicate parent beforeLoad/loaders, verifying TanStack Router/Start/AI against current docs, or enforcing server/client execution boundaries (isomorphic loaders, import protection, middleware-inferred request context). For logging, error tracking, env schemas, or shellSession setup, load companion skill observability-and-env instead. For this template's concrete package defaults, load companion skill reference-tech-stack.",
      "projectName": "TanStack AI-Promptable Full-Stack Template",
      "projectSummary": "A production-ready TanStack Start template designed to make internal tools AI promptable by default.",
      "version": "1.28.1",
      "author": {
        "name": "Carlos Martin-Sanchez",
        "url": "https://github.com/carlosvin"
      },
      "license": "MIT",
      "homepage": "https://github.com/carlosvin/tanstack-fullstack-ai-template",
      "repository": "https://github.com/carlosvin/tanstack-fullstack-ai-template",
      "documentationUrl": "https://github.com/carlosvin/tanstack-fullstack-ai-template/blob/main/skills/README.md",
      "status": "stable",
      "supportedTools": [
        {
          "id": "windsurf",
          "name": "Windsurf",
          "installMode": "native",
          "tested": true,
          "notes": "Reads `.agents/skills/` directly when the skill lives in the project."
        },
        {
          "id": "cursor",
          "name": "Cursor",
          "installMode": "copy",
          "tested": true,
          "notes": "Copy the generated skill folder into the global Cursor skills directory."
        },
        {
          "id": "claude-code",
          "name": "Claude Code",
          "installMode": "copy",
          "tested": true,
          "notes": "Copy the generated skill folder into the Claude Code skills directory."
        }
      ],
      "tags": [
        "tanstack-start",
        "fullstack",
        "architecture",
        "interface-first",
        "repository-pattern",
        "ai-promptable"
      ],
      "capabilities": [
        "Interface-first boundaries with swappable implementations",
        "Three schema layers with mandatory Schema.parse() at every boundary (tool→repo and repo→tool)",
        "Strong TypeScript in the typed flow — inference preserved via satisfies, unions, exhaustive switches; casts minimized",
        "Loader-first routes and URL-driven state via validateSearch",
        "Router config bundle (defaults + project Link wrapper preserving search params)",
        "Full AI tool coverage mirroring repository surface + client navigate/invalidate tools",
        "Schema-first AI/UI metadata (.describe + optional .meta for unit/format/title)",
        "Promptable by default — getAIAvailability gating + browserContext + bounded agent loop",
        "Auth ticket built in middleware via repository + TraceabilityContext on writes",
        "Parent layout routes deduplicating shared beforeLoad and loaders",
        "Optional patterns — overlay repo, bulk edit, distinct-values tools, dynamic route introspection, debounced free-text search, optional mobile-first layout (ask when the UI library makes it unclear or hard)",
        "TanStack Intent + CLI as doc-aligned guidance (not duplicated command manuals)",
        "Assistant chat renders Markdown (GFM) — lists, tables, code blocks; internal links stay navigable",
        "Server/client execution boundaries — isomorphic loaders, *.server.ts, createServerOnlyFn, import protection",
        "Request context — middleware-inferred ctx.context, parse-don't-validate inside handlers; env/shellSession invariants (setup in observability-and-env)"
      ],
      "triggers": [
        "fullstack template",
        "TanStack Start project",
        "repository pattern",
        "interface-first",
        "new app scaffold",
        "nested routes",
        "layout route",
        "beforeLoad",
        "tanstack cli",
        "tanstack intent",
        "package skills",
        "client bundle leak",
        "server-only",
        "isomorphic loader",
        "process.env in loader",
        "import protection",
        "request context",
        "middleware context",
        "debounced search",
        "search input",
        "mobile first"
      ],
      "inputs": [
        "Existing or new TanStack Start codebase",
        "Domain requirements for data model and auth"
      ],
      "outputs": [
        "Interface-first architecture with swappable implementations",
        "Loader-first routes and URL-driven state conventions"
      ],
      "constraints": [
        "External services are accessed through interfaces only",
        "Domain wire shapes come from runtime validation schemas (Zod, ArkType, Valibot, or equivalent); infer types from schemas — repository interfaces use repository-layer types only",
        "Tools-layer schemas shared by createServerFn and AI tools; repository interfaces use repository-layer types only; UI and AI never import repository schemas",
        "Map inbound with RepoInputSchema.parse(fromTools(...)) and outbound with ToolsOutputSchema.parse(fromRepo(...)) so wire shapes never bypass validation",
        "After Schema.parse, preserve inferred types end-to-end — avoid widening to any or Record<string, unknown>; avoid as casts except at documented third-party boundaries; prefer satisfies, discriminated unions, const tuples, type guards, and exhaustive handling",
        "Fetch route data in loaders through server functions; keep route files thin; URL state in validateSearch with loaderDeps",
        "Never put DB drivers, repository imports, or secret process.env reads in route loaders or route top-level imports; use createServerFn from serverFns.ts and *.server.ts modules",
        "POST mutations chain `.middleware([requireAuthMiddleware, invalidateMiddleware])`; handlers throw HttpError; callers normalize via processResponse / safeToolHandler / createSafeServerTool",
        "Expose every repository method as a safe AI tool; distinct-values tools for enum-ish filters",
        "Attach human-readable descriptions to schema fields for AI/UI metadata (reference — Zod `.describe()`; ArkType and Valibot have equivalent metadata APIs); use structured schema extras only for non-description hints (unit, format, title, …); closed vocabularies via const tuple + schema enum + inferred type",
        "Prefer schema and router introspection over hand-maintained AI manifests; export JSON Schema from your validator when the UI needs it (reference — `z.toJSONSchema()` with Zod)",
        "Shared route gates and expensive reads live on the parent layout route; use a project Link that preserves search params by default",
        "Router defaults bundle — defaultStaleTime, defaultPreload 'intent', defaultPreloadStaleTime 0, scrollRestoration, notFoundComponent — ship together",
        "Auth middleware enriches ctx.context with a repository-built ticket (e.g. getReadRepository().getUserAccess(email)); WritableRepository writes accept a TraceabilityContext built from that ticket",
        "Promptable by default — root loader checks getAIAvailability(); chat input includes browserContext (timezone, locale, path); chat() uses agentLoopStrategy maxIterations(N)",
        "Verify TanStack Router/Start/AI APIs with npx @tanstack/cli against current docs; use TanStack Intent for package skills — do not add them as project deps unless the app needs them as tooling",
        "Operational detail — UI library, chat Markdown rendering (implementation in AGENTS.md §8), logging, error tracking, testing, env bridge — follows repo AGENTS.md",
        "Request context — chain middleware for inferred context.* types; never leak serverEnv to the client (browser config only via getBrowserShellSession / shellSession). Env parse/bootstrap/logging/error-tracking setup lives in companion skill observability-and-env — do not duplicate here."
      ],
      "steps": [
        "Define repository-layer and tools-layer schemas; implement repository interfaces (seed + production)",
        "Map between layers with Schema.parse() at boundaries; carry schema-inferred types through handlers, UI, and tools without erasing to any or untyped records",
        "Add GET/POST server functions with shared tools-layer input validators",
        "Register global middleware — auth (enrich ctx.context with a repository-built ticket), invalidation; optional pre-auth redirects",
        "Configure router defaults bundle and project Link wrapper; add routes with validateSearch, loaderDeps, loaders; nest shared work on parent layouts",
        "Wire AI — toolDefinition for every server function, safe handlers, client navigate/invalidate tools, buildSystemPrompt fed by the auth ticket and chat browserContext, agentLoopStrategy maxIterations(N) on chat(); ensure assistant messages render as Markdown per AGENTS.md §8",
        "Gate chat UI on getAIAvailability() in the root loader; add distinct-values read paths for enum-ish filters when applicable",
        "Validate changes per AGENTS.md; use TanStack CLI/Intent when changing Router/Start/AI behavior"
      ],
      "examples": [
        {
          "input": "Add a new domain entity to the template",
          "output": "Update schema, repository interfaces, server functions, routes, and AI tools"
        },
        {
          "input": "Swap MongoDB to Postgres",
          "output": "Implement repository interface and switch factory without app-level changes"
        },
        {
          "input": "Layout should work on small screens",
          "output": "Optional mobile-first design — start from the narrow viewport and enhance as width grows; ask if the chosen UI library makes that unclear or hard"
        }
      ],
      "companionSkills": [
        {
          "id": "observability-and-env",
          "relationship": "companion",
          "summary": "Logging, error-tracking bootstrap, env schemas, webEnvMiddleware, and shellSession setup. Install when work touches observability, process.env, or browser runtime config."
        },
        {
          "id": "reference-tech-stack",
          "relationship": "companion",
          "summary": "Opinionated vendor map for this template's reference app. Install when implementing against the demo stack defaults (UI kit, validator, DB, deploy)."
        }
      ],
      "source": "skills/src/tanstack-promptable-fullstack-app-template.skill.yaml",
      "outputsByFormat": {
        "skill": ".agents/skills/tanstack-promptable-fullstack-app-template/SKILL.md",
        "markdown": "skills/dist/tanstack-promptable-fullstack-app-template.md"
      }
    }
  ]
}
