id: reference-tech-stack-lookup
name: Reference tech stack lookup
description: Covers USE FOR reference tech stack and which UI library.
tags: [reference, trigger]
inputs:
  prompt: What is the reference tech stack, and which UI library does this template use?
expected:
  outcomes:
    - type: task_completed
graders:
  - name: has_output
    type: code
    config:
      assertions:
        - "len(output) > 10"
