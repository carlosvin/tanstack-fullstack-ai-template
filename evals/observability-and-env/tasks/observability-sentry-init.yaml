id: observability-sentry-init
name: Error tracking bootstrap
description: Covers USE FOR sentry init.
tags: [observability, trigger]
inputs:
  prompt: Wire sentry init through ObservabilityService and instrument.server.mts, not inside a route loader.
expected:
  outcomes:
    - type: task_completed
graders:
  - name: has_output
    type: code
    config:
      assertions:
        - "len(output) > 10"
